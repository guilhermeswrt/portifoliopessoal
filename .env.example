# Copie este arquivo para `.env` e preencha os valores.
# Obs: `.env` fica ignorado no git (veja `.gitignore`).

# API
PORT=3001
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173

# GitHub (para listar repos e disparar workflows)
GITHUB_USERNAME=
GITHUB_TOKEN=
GITHUB_REPOS_LIMIT=6
GITHUB_WORKFLOW_FILENAME=ci.yml
# Repo onde está o workflow central (este portfólio). Ex: guilhermeswrt/portifoliopessoal
GITHUB_WORKFLOW_REPO=guilhermeswrt/portifoliopessoal

# Branch/ref do repo do workflow central (normalmente master)
GITHUB_WORKFLOW_REF=master

# Exemplo de como gerar um token: https://docs.github.com/pt/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token