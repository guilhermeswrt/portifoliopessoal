{
  "name": "portifoliopessoal",
  "version": "1.0.0",
  "description": "Projeto para divulgar currículo e projetos de forma segura, sem login, com backend em Docker.",
  "homepage": "https://github.com/guilhermeswrt/portifoliopessoal#readme",
  "bugs": {
    "url": "https://github.com/guilhermeswrt/portifoliopessoal/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/guilhermeswrt/portifoliopessoal.git"
  },
  "license": "ISC",
  "author": "Siewert, Guilherme",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:all",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "npm --prefix frontend test",
    "test:all": "npm run test:backend && npm run test:frontend",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed --workers=1",
    "test:e2e:ui": "playwright test --ui --headed --workers=1",
    "e2e:servers": "cross-env PORTFOLIO_DATA=test E2E_MODE=1 PORT=3001 ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173 concurrently -k -s first \"node backend/src/server.js\" \"npm --prefix frontend run dev\"",
    "prestart": "node scripts/github-auth.js",
    "predev:start": "node scripts/github-auth.js",
    "dev:backend": "cd backend && npm install && npm run dev",
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:start": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "docker compose up --build",
    "prod:frontend": "cd frontend && npm install && npm run dev",
    "prod:backend": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "cross-env": "^7.0.3"
  }
}
